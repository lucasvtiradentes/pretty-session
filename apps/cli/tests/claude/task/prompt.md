Use the TaskCreate tool to create a task with subject "Fix login bug" and description "Investigate and fix the login bug". Then use TaskUpdate to mark task #1 as completed. Do nothing else.

NOTE: TaskCreate/TaskUpdate are only available in interactive CLI mode, not in `claude -p` mode.
This test uses hand-crafted fixtures since e2e generation is not possible.